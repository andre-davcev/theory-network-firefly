import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.theorynetwork.Firefly',
  appName: 'Firefly',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
