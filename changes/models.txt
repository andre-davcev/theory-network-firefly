FirebaseDocument
    version     : string;
    id          : string;
    userId      : string;
    dateCreated : firestore.FieldValue;
    dateUpdated : firestore.FieldValue;
    metadata?   : Record<string, any>;

(move from models to documents)

Alert extends FirebaseDocument
    name        : string;
    description : string;
    bucketPath  : string;
    eventId     : string;
    clusterId   : string;
    dateTime    : firestore.FieldValue;
    url?        : string;
    read        : boolean;

    imageUrl?     : string;
    dateTimeDate? : Date;

Beacon extends FirebaseDocument
    name        : string;
    tagline     : string;
    description : string;

    major       : number;
    majorLabel  : string;
    minor       : number;
    minorLabel  : string;
    uuid        : string;
    uuidLabel   : string;

Cluster extends FirebaseDocument
    name            : string;
    tagline         : string;
    description     : string;
    bucketPath      : string;
    subscriberCount : number;
    tokenPage       : number;
    tokenPageCount  : number;

Event extends FirebaseDocument
    name            : string;
    tagline         : string;
    description     : string;
    bucketPath      : string;
    geopoint        : firestore.GeoPoint;
    location        : Location;
    city            : Location;
    cityId          : string;
    timeStart       : string;
    timeEnd         : string;
    clusters        : Array<string>;
    notifyCompleted : boolean;
    notifyImmediate : boolean;                                           // can't edit if notifyCompleted == true
    notifyDateTime  :  firestore.Timestamp; (Limit to hour increments)   // can't edit if notifyCompleted == true

Icon extends FirebaseDocument
    name       : string;
    bucketPath : string;
    mediaType  : string;

Image extends FirebaseDocument
    name       : string;
    bucketPath : string;
    mediaType  : string;

StreamItem extends FirebaseDocument
    subscribed: boolean;

Subscription:
    on:     boolean;

User
    ...User
    providerId  : string;
    language    : string;
    email       : string;
    phoneNumber : string;

    location     : Location;
    city         : Location;
    dateLoggedIn : firestore.Timestamp;
    profile      : UserProfile;
    roleEditors  : Record<string, string>;
    roleAdmins   : Record<string, string>;
    subscriptions: Record<{clusterId}, Subscription>;
    notifications : boolean;
    tokens       : Array<string>;
