alerts (functions create)
  if events added/removed
    get cluster-city
    create/update ClusterCity stats
app (forms)
  required fields styling
app (lists)
  empty icon/message for no data screens
app (menu)
  move menu selection nav to root
  add home section to menu
  when menu open
    disable go button
    disable alert detail navigation
capacitor (push notifications)
  implement capacitor-fcm plugin
clusters (create/edit)
  event add/remove workflow
clusters (functions create/update)
cluster-subscribers (functions update)
  paging of collections (limit 15000)
  [${clusterId}_{pageNumber}]
    [userId] :
    {
      tokens: Array<{tokenId}>
    }
coding (functions)
  minimize cold start time
  refactor to use try/catch async/await
console (functions settings)
  enable retry on failure
events (list)
  list image/name/timeStart/timeEnd
events (edit)
  reverse geocode

events (functions cron)
  cron hourly 55 mins past hour
  find hour past current time
  query events whose notifiedDateTime === generatedHourlyTimestamp
  for each cluster id
    get cluster?
    get cluster.subscriberPageCount?
    forkJoin(cluster-subscribers/{clusterId}_{pageNumber})
      get users/{subscriberId}
        save event info to alerts where tokens is empty if !user.pushNotifications
        send push notification if alert.tokens.length > 0
icons (grid)
  create grid view
icons (create/edit)
  add description back
icons (functions delete)
  delete icon from storage
images (grid)
  create grid view
images (create/edit)
  add description back
images (functions delete)
  delete image from storage
ios (dev)
  chrome debugging
library (firebase)
  StateFirestore
    ActionPersistenceOn
    ActionPersistenceOff
library (ngxs)
  child ActionSetData (user/{userId}/subscriptions)
library (mapbox)
  add back tokenized mapbox access token in search service
  create search bar which limits by mapbox types ['poi', 'address']
library (services)
  cleanup
location (state)
  on location found
    search mapbox reverse geocode for city latitude/longitude
    search bigdatacloud for latitude/longitude city data
    save to user.location
map (component)
  limit mapbox search types to [address, poi]
notifications
  capacitor-fcm plugin
  add unread badge count
streams (functions cron)
  get users collection
  for each user logged in the past 30 days
    get user.city
    get cities/{cityId}
    push city to array
    loop through city.nearby sorted by shortest distance
      push to city array
    loop through city.clusters
      generate score using:
        event count
        event ratio
        distance ranking
      sort list based on score descending
subscriptions (list)
  migrate to use user.subscriptions instead of user-subscriptions
  create with on/off switch
users (state)
  setup user object watch
users (functions create)
  generate and add first alert
users (functions update)
  if token count passes 10
    delete oldest
  if subscription turned on
users (functions create/update)
  if dateLoggedIn & city changed then generate new stream
user-roles (functions)
  [delete]
user-profiles (functions)
  [delete]
