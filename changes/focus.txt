users (state)
  migrate to use StateDocument
users (app)
  on location change
    update user
    {
      location,
      cityId
    }
    query streams/{user.cityId}
  on login & location found
      update user:
      {
          dateLoggedIn,
          location,
          cityId
      }
library (ngxs)
  child ActionSetData (user/{userId}/subscriptions)
streams (all)
  streams (cron)
  streams (state)
  streams (app)
functions (test)
  clusters (delete)
  events (update)
  icons (delete)
  images (delete)
  users (create)
  users (delete)
  users (update)
ios (dev)
  chrome debugging
