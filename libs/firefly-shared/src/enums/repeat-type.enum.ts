export enum RepeatType
{
    Never    = 'never',
    Daily    = 'daily',
    Weekdays = 'weekdays',
    Weekends = 'weekends',
    Weekly   = 'weekly',
    Monthly  = 'monthly',
    Yearly   = 'yearly',
    Custom   = 'custom'
}
