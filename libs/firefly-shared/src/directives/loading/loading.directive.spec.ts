// import { DirectiveLoading } from './loading.directive';

describe('DirectiveLoading', () => {
  /*
  it('should create an instance', () => {
    const directive = new DirectiveLoading();
    expect(directive).toBeTruthy();
  });
*/
});
